#include "../cuda_sort.hpp"
#include <vector>

namespace tobi {
	namespace cuda {
		namespace sort {
			//thrust::sort()
			template<class T>
			int sort(std::vector<T>& vector)
			{
				
			}
		}
	}
}